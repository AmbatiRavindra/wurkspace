/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  // we need to specify domain name of the images in the domains array, if we get those images from the sites...
  images: {
    domains: []
  }
}
